# @anlyx/core

Core Project JSON schemas, validation, normalization, and local viewer data
contracts for Anlyx.

The public product path is `anlyx.project.json -> validate -> import -> local
4777 viewer`.
