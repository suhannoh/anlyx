# @anlyx/adapter-manual

Manual project metadata helpers for Anlyx.

Project JSON is the primary product path. This package is a producer/helper
surface for explicit manually-authored page or URL metadata; it is not required
for the local 4777 viewer to render a valid `anlyx.project.json`.
